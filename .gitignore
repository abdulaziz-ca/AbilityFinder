# macOS
.DS_Store

# Node / Wrangler (Phase 4)
node_modules/
.wrangler/
test-results/
playwright-report/

# Secrets — never commit. Wrangler reads .dev.vars for local dev.
.dev.vars
.env
*.local

# Temporary a11y audit tool — never ship
public/axe.min.js
# TaskView local state (shared policy and templates remain tracked)
.taskview/offline-log.md
.taskview/local.json

# Owner-local project docs — intentionally untracked (ticket #103); never commit
WEBSITE-PROJECT-TRACKER.md
FABSOL-RESUME-PROMPT.md
