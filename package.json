{
  "name": "abilityfinder",
  "version": "1.0.0",
  "private": true,
  "description": "AbilityFinder — every disability benefit you can get in Alberta",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
